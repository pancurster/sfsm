PROJECT(sfsmtest CXX)

INCLUDE_DIRECTORIES(${sfsm_SOURCE_DIR}/src)
LINK_DIRECTORIES(${sfsm_BINARY_DIR}/src)

ADD_EXECUTABLE(run_test test.cpp)

SET(CMAKE_CXX_FLAGS -g)
SET(EXECUTABLE_OUTPUT_PATH ${sfsm_BINARY_DIR})

TARGET_LINK_LIBRARIES(run_test sfsm CppUTest CppUTestExt)

